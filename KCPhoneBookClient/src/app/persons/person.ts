/* Defines the product entity */
export interface IPerson {
    id: number | null;  
    title: string;
    firstName: string;
    lastName: string;
    email: string;
    address: string;   
    homeNumber:number;   
    workNumber: number;  
}

